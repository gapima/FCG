# Inicia os serviços (PostgreSQL + API)
docker-compose up -d

# Verifica status
docker-compose ps

# Logs da API
docker-compose logs -f api

# Acessa o Swagger
# http://localhost:5080/swagger

# Health check
# http://localhost:5080/health

# Para a aplicação
docker-compose down
